apiVersion: apps/v1
kind: Deployment
metadata:
  name: "fridgy"
spec:
  selector:
    matchLabels:
      app: "fridgy"
  template:
    metadata:
      labels:
        app: "fridgy"
    spec:
      containers:
        - name: "fridgy"
          image: "gcr.io/fridgy/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "fridgy"
spec:
  selector:
    app: "fridgy"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080